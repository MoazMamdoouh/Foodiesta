<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Foodiesta" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/purple</item>
    </style>
    <style name="Theme.Foodiesta" parent="Base.Theme.Foodiesta" />
    <style name="CircularImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <style name="ButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/search_btn_background</item>
        <item name="android:backgroundTint">@null</item> <!-- Remove tint to allow drawable -->
        <item name="android:textColor">#F8E4B0</item> <!-- Change text color if needed -->
    </style>
    <style name="MyDatePickerStyle" parent="android:Widget.Material.DatePicker">
        <item name="android:background">#FFDDDD</item>
        <item name="android:textColorPrimary">#000000</item>
    </style>
</resources>